# Mirror repository

Read-only public mirror of the **AddMaple Python client**. This tree is refreshed
automatically; do not open pull requests here.

Install: `pip install addmaple` ([PyPI](https://pypi.org/project/addmaple/)).
